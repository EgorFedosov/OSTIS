# Partners Processing Module (Вариант 15)

Модуль для интеллектуальной системы OSTIS, реализующий операции над множествами партнёров компаний.

## 1. Агенты

В модуле реализованы два C++ агента, работающие с базой знаний:

### 🔴 FindIntersectionAgent (Поиск пересечения)
**Задача:** Найти общих бизнес-партнёров для двух компаний.
* **Вход:** Множество, содержащее две компании (аргументы `rrel_1` и `rrel_2`).
* **Логика:**
    1.  Находит всех партнёров первой компании.
    2.  Для каждого партнёра проверяет, есть ли у него связь со второй компанией.
    3.  Если связь есть — добавляет партнёра в результат.
* **Результат:** SC-структура, содержащая только общих партнёров.

### 🔵 FindUnionAgent (Поиск объединения)
**Задача:** Сформировать единый список всех уникальных партнёров двух компаний.
* **Вход:** То же самое (две компании).
* **Логика:**
    1.  Собирает всех партнёров первой компании.
    2.  Собирает всех партнёров второй компании.
    3.  Использует хеш-сет (`std::set`), чтобы исключить дубликаты (если партнёр работает с обеими, он будет добавлен один раз).
* **Результат:** SC-структура, содержащая полный список партнёров.

---

## 2. Тестирование (Unit Tests)

Тесты написаны на C++ с использованием фреймворка **Google Test**. Они работают в полностью изолированной памяти (`ScMemoryTest`), не требуя запуска сервера и загрузки файлов `.scs`.

### Как работают тесты:

1.  **Подготовка данных (Setup):**
    В коде теста программно создается граф:
    * **Компании:** Alpha, Beta.
    * **Партнёры:** Google, Microsoft, Yandex, Tesla.
    * **Связи:**
        * Alpha работает с: {Google, Microsoft, Yandex}.
        * Beta работает с: {Microsoft, Yandex, Tesla}.

2.  **Сценарии проверки:**
    * **Intersection Test:** Запускает первого агента и проверяет, что в ответе ровно **2** узла (Microsoft и Yandex).
    * **Union Test:** Запускает второго агента и проверяет, что в ответе ровно **4** узла (все уникальные).
    * **Error Test:** Запускает агента без аргументов и проверяет, что он корректно возвращает ошибку (`FinishWithError`), а не падает.

3.  **Верификация:**
    Используются `EXPECT_TRUE` и итераторы для подсчета количества элементов в ответе агента.